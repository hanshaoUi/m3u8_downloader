# 猫抓 + M3U8下载器集成使用说明

## 🚀 快速开始

### 1. 启动M3U8下载器
```bash
python main.py
```
启动后自动开启HTTP服务器在端口8080，GUI界面会显示"HTTP服务器：运行中"。

### 2. 配置猫抓外部下载工具

在猫抓扩展设置中添加外部下载工具：

**配置信息：**
- **名称**: `M3U8下载器`
- **命令**:
```bash
curl -X POST http://127.0.0.1:8080/api/add-download -H "Content-Type: application/json" -d "{\"url\":\"%URL%\",\"filename\":\"%FILENAME%\"}"
```

### 3. 使用流程

1. 在浏览器中访问包含视频的网页
2. 打开猫抓扩展，等待检测M3U8链接
3. 点击检测到的链接旁的"外部下载"或"发送到M3U8下载器"
4. 下载任务会自动出现在M3U8下载器的GUI界面中
5. 可以在GUI中查看下载进度和管理任务

## 🛠️ 其他使用方法

### 方法1：浏览器控制台
在检测到M3U8链接后，打开开发者工具(F12)，在Console中执行：

```javascript
fetch('http://127.0.0.1:8080/api/add-download', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    url: '这里替换为M3U8链接',
    filename: '自定义文件名'
  })
}).then(r => r.json()).then(d => console.log(d));
```

### 方法2：浏览器书签快捷方式
创建浏览器书签，一键发送链接：

**书签名称**: `发送到M3U8下载器`
**书签地址**:
```javascript
javascript:(function(){var url=prompt('请输入M3U8链接:');if(url){fetch('http://127.0.0.1:8080/api/add-download',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url:url,filename:''})}).then(r=>r.json()).then(d=>alert(d.success?'添加成功!':'添加失败: '+d.error));}})();
```

## 📋 API接口文档

### 添加下载任务
```http
POST /api/add-download
Content-Type: application/json

{
  "url": "https://example.com/video.m3u8",
  "filename": "my_video"  // 可选，默认自动生成
}
```

**成功响应**:
```json
{
  "success": true,
  "task_id": "e935d2ec-4ada-4370-b046-a08ca1d64dac",
  "filename": "my_video",
  "message": "下载任务已添加并开始下载"
}
```

### 获取服务器状态
```http
GET /api/status
```

### 获取所有任务
```http
GET /api/tasks
```

### Web管理界面
访问 `http://127.0.0.1:8080/` 可以使用Web界面手动添加下载任务。

## ⚙️ 配置设置

### 修改服务器端口
如果8080端口被占用，可以修改配置文件：

配置文件位置：
- Windows: `%USERPROFILE%\.m3u8_downloader\config.json`
- macOS/Linux: `~/.m3u8_downloader/config.json`

```json
{
  "browser": {
    "server_port": 8080,
    "auto_start_server": true
  }
}
```

### 不同猫抓版本的配置方法

**CatCatch 扩展**:
1. 右键点击扩展图标 → 选项
2. 设置 → 下载 → 外部下载工具
3. 添加工具，使用上述命令配置

**Video DownloadHelper**:
1. 扩展设置 → Advanced → Custom converter
2. 添加自定义转换器，配置API调用

## 🔧 故障排除

### 常见问题及解决方法

**1. 猫抓显示"外部工具调用失败"**
- 检查M3U8下载器是否正在运行
- 尝试访问 `http://127.0.0.1:8080/` 确认服务器可用
- 检查Windows防火墙是否阻止了连接

**2. 端口被占用**
- 修改配置文件中的端口号
- 或者关闭占用8080端口的其他程序

**3. 下载任务不出现在GUI中**
- 确保使用的是默认启动模式 `python main.py`
- 检查程序日志是否有错误信息

**4. 某些M3U8链接无法下载**
- 可能需要特殊的headers或cookies
- 尝试在GUI中手动添加，并设置自定义headers

### 测试API连接
使用curl命令测试API是否正常：

```bash
# 测试服务器状态
curl http://127.0.0.1:8080/api/status

# 测试添加下载（使用示例链接）
curl -X POST http://127.0.0.1:8080/api/add-download \
  -H "Content-Type: application/json" \
  -d '{"url":"https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8","filename":"test_video"}'
```

## 🎯 使用优势

相比自己开发浏览器扩展，这种集成方式有以下优势：

✅ **利用猫抓成熟的检测能力** - 无需重复开发M3U8检测逻辑
✅ **支持多种浏览器** - Chrome、Firefox、Edge等都可以使用
✅ **配置简单** - 只需添加一个外部工具配置
✅ **功能完整** - 获得专业下载器的全部功能
✅ **实时同步** - GUI和API任务完全同步显示
✅ **维护简单** - 无需更新浏览器扩展

## 📞 获取帮助

如果遇到问题：
1. 查看程序控制台输出
2. 检查日志文件：`~/.m3u8_downloader/logs/`
3. 确认API接口响应是否正常

---

🎉 **现在你可以享受猫抓强大的检测能力 + M3U8下载器的专业下载功能！**